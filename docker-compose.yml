version: "3.3"

services:
  authentication:
    build: authentication/.
    image: localhost:5002/auth-im
    ports:
      - "3000"
    networks:
      - backend
  registration:
    image: localhost:5002/reg-im
    build: registration/.
    ports:
      - "3000"
    networks:
      - backend
  patientdata:
    image: localhost:5002/pd-im
    build: patientdata/.
    ports:
      - "3000"
    networks:
      - backend    
  

networks:
  backend:
    driver: bridge
